export interface Review{
    rating: number;
    rideId: number;
    comment: string;
    driver: number;
}
export interface ReviewDTO{
    rating: number;
    ride: number;
    comment: string;
    driver: number;
}
